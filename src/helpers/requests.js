const requests = {
  fetchPlanets: `planets/?format=json`,
  fetchFilms: `films/?format=json`,
  fetchPeople: `people/?format=json`,
  fetchAllPlanets: `planets/?page=2&format=json`,
}

export default requests
